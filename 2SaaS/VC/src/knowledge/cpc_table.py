"""카테고리별 CPC 적정 단가표 (prompts/solution_planning.md 기준).

※ 신규 업종(카페·디저트, 버거·샌드위치, 한식, 야식·안주, 아시안 등)의 단가는
  실제 배민 CPC 데이터 미수집 상태이므로 업종 특성·기존 값 일관성 기준의 추정값.
  실데이터 수집 후 재조정 필요.
"""

CPC_BASE: dict[str, int] = {
    # ⚠️ 순서 규칙: 복합 키("카페·디저트", "버거·샌드위치" 등)를
    # 단순 키("카페", "버거")보다 먼저 배치해야 cpc_for()의 부분 매칭
    # 루프가 정확 매칭을 우선한다.

    # ── 저가 구간 (200~300원대): 객단가 낮거나 회전 빠름 ──
    "카페·디저트": 200,  # ※ 추정값 — 저객단가·고회전
    "카페": 200, "디저트": 200,
    "국수": 288, "밀면": 288, "냉면": 288,
    "덮밥": 288, "서브 브랜드": 288,
    "한식": 280,  # ※ 추정값 — 일상식, 동네 단골 중심
    "백반": 280, "국밥": 280,  # ※ 추정값
    "아시안": 300,  # ※ 추정값 — 쌀국수·팟타이 등
    "베트남": 300, "태국": 300, "인도": 300,  # ※ 추정값

    # ── 중가 구간 (320~400원대): 주력 배달 업종 ──
    "중식": 320, "중화요리": 320, "짜장": 320, "짬뽕": 320,  # ※ 추정값 — 중식 전반
    "야식·안주": 330,  # ※ 추정값 — 야간 수요 (복합 키 우선)
    "야식": 330, "안주": 330,
    "족발·보쌈": 350,  # ※ 추정값 — 업종 평균 (단일 "족발"/"보쌈" 551과 구분)
    "버거·샌드위치": 380, "샌드위치": 380,  # ※ 추정값 — 간편식
    "파스타": 351, "양식": 351, "버거": 351,
    "분식": 388, "떡볶이": 388, "치킨": 388,
    "돈까스": 388, "회": 388, "일식": 388, "카레": 388,

    # ── 고가 구간 (450원대 이상): 객단가 높고 모임 중심 ──
    "피자": 450,  # ※ 추정값 — 기존 451→450 조정, 광고 경쟁 치열
    "찜탕": 451, "감자탕": 451, "낙지": 451,
    "족발": 551, "보쌈": 551, "낙곱새": 551, "메인 브랜드": 551,
}


def cpc_for(cuisine: str) -> tuple[int, int]:
    """업종명 → (기본 CPC, 단계적 상향 CPC).
    보정: 신규 +50, 서울 핵심 +50 (여기선 안전하게 + 50 한 번만)"""
    base = 388  # default
    for key, price in CPC_BASE.items():
        if key in cuisine:
            base = price
            break
    return base, min(base + 50, 600)
